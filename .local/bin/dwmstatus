#!/usr/bin/env bash

# For dwmstatus
while true; do
    xsetroot -name " $($HOME/bin/network-wlan-tfc.sh)  $($HOME/bin/cpu-temp.sh)°C  $($HOME/bin/memory-mod.sh)%  $($HOME/bin/filesystem-mod.sh)  $($HOME/bin/volume-mod.sh) $(date +" 0%u%y%m%d%H%M") $($HOME/bin/bat-state.sh) $($HOME/bin/bat-capacity.sh)%  BANDITHIJO "
	sleep 1
done
