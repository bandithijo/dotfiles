#!/usr/bin/env bash

# For dwm status
while true; do
    xsetroot -name "$($HOME/bin/status/vpn)$($HOME/bin/status/screenkey)$($HOME/bin/status/record)$($HOME/bin/status/network-wlan-tfc) $($HOME/bin/status/cpu-temp) $($HOME/bin/status/memory) $($HOME/bin/status/filesystem) $($HOME/bin/status/volume) $(date +" 0%u%y%m%d%H%M") $($HOME/bin/status/bat-state) $($HOME/bin/status/bat-capacity)  BANDITHIJO"
    # sleep 0.5
done
